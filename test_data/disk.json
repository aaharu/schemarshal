{
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "enum": [ "disk", "disk2" ]
    },
    "device": {
      "type": "string",
      "pattern": "^/dev/[^/]+(/[^/]+)*$"
    }
  },
  "required": [ "type", "device" ],
  "additionalProperties": false
}
